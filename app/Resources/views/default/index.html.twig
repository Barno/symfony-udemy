{% extends 'base.html.twig' %}

{% block body %}
    <h1>Master</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Invia</button>
    {{ form_end(form) }}
{% endblock %}
